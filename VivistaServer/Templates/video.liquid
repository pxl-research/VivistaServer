<div class="pure-g center">
	<div class="video-info-image pure-u-1">
		<div class="download-message">
			<button data-uri="vivista://video/{{video.id | guidbase64 }}" class="pure-button download-button">
				<span class="play-icon">▶</span> Play In Vivista
				<p class="hidden install-message">Don't have Vivista installed? <a href="/install/latest" download>Download now</a></p>
			</button>
		</div>
		<img class="pure-img" src="/api/thumbnail?id={{ video.id }}">
	</div>
	<div class="video-info pure-u-1 pure-u-md-1-2">
		<h1 class="title">{{ video.title }}</h1>
		<p class="view-stats">{{ video.length | format_seconds }} • {{video.downloadsize | format_bytes }} • {{ video.views | format_number: "N0" }} views • {{ video.downloads | format_number: "N0" }} downloads</p>
		<p class="description">Test description{{ video.description }}</p>
		<p>
			{% for tag in video.tags %}
				<span class="tag">{{ tag.name }}</span>
			{% endfor %}
		</p>
	</div>
	<div class="related-videos">
		<h1>Related videos</h1>
		{% for v in relatedVideos %}
		<div class="video pure-g">
			<div class="pure-u-1-3">
				<img class="pure-img" src="thumb.png">
			</div>
			<div class="pure-u-2-3">
				<p class="title">Title</p>
				<p class="uploader">User </p>
			</div>
		</div>
		{% endfor %}
		{% for v in video.relatedVideos %}
		{% endfor %}
	</div>
</div>
